[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=/app/backend /opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
