# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database — 2.28+ corrige incompatibilidad gotrue/httpx (keyword "proxy")
# NOTA: Usamos supabase 2.3.4 porque versiones 2.28+ requieren pyiceberg que necesita Visual Studio Build Tools
supabase==2.3.4
# `supabase==2.3.4` depende de `httpx<0.26`. Además, `gotrue>=2.9` usa `proxy=` (requiere httpx>=0.26),
# así que fijamos `gotrue` a una versión compatible con httpx 0.25.x.
gotrue==2.8.1
httpx==0.25.2
# Python 3.14: httpcore 1.0.8+ evita el crash por typing.Union (SOCKET_OPTION).
# Fijamos versión para forzar reinstalación limpia si alguien parcheó site-packages.
httpcore==1.0.9
websockets>=11,<13

# Payment integration
mercadopago==2.2.1

# Data validation (use 2.10+ for prebuilt wheels on Python 3.13+)
pydantic>=2.10.4,<3
pydantic-settings>=2.6.0
email-validator>=2.2.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
hypothesis==6.98.3

# Development
python-dotenv==1.0.0

# Document processing
python-docx==1.1.0
openpyxl==3.1.2
antiword==0.0.1  # Para archivos .doc antiguos
textract==1.6.5  # Alternativa para extraer texto de .doc
